# Deployment Guide

Set this environment variable wherever you deploy the backend:

```env
SERPAPI_KEY=your_serpapi_key_here
```

Then run:

```bash
pip install -r requirements.txt
python3 pyscrpt.py
```
